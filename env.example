# Server configuration
HOST=0.0.0.0
PORT=8000
RELOAD=false

# MVG API configuration
MVG_API_TIMEOUT=10
MVG_API_LIMIT=20
MVG_API_OFFSET_MINUTES=0

# Display configuration
TIME_FORMAT=minutes
REFRESH_INTERVAL_SECONDS=30
PAGINATION_ENABLED=true
DEPARTURES_PER_PAGE=5
PAGE_ROTATION_SECONDS=8
THEME=light

# Banner/header background color (hex color code, e.g., #087BC4)
BANNER_COLOR=#087BC4

# Configuration file (required)
# Path to TOML configuration file for stop mappings and display settings
# Defaults to config.example.toml if not set
# CONFIG_FILE=config.toml
#
# See config.example.toml for the configuration format.
# To find a station ID, run: python scripts/find_station.py "Station Name" München

